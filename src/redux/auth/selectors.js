export const selectIsLoggedIn = (state) => state.isLoggedIn;
export const selectIsError = (state) => state.isError;
export const selectUser = (state) => state.user;
export const selectIsRefresh = (state) => state.isRefresh;
